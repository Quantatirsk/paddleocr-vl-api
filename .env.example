# VLLM Server 配置
VLLM_SERVER_URL=http://vllm-server:8000/v1

# OCR API 性能配置
MAX_WORKERS=4          # Gunicorn 进程数（建议 CPU cores * 2 + 1）
THREAD_WORKERS=10      # 每个进程的线程池大小

# GPU 配置（多 GPU 部署时使用）
NVIDIA_VISIBLE_DEVICES=0

# 日志级别
LOG_LEVEL=INFO
